{
  "name": "denivele",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DÉNIVELÉ — planification trail + PPG, données Coros",
  "scripts": {
    "dev": "npm run build:club && vite",
    "build": "npm run build:club && vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "server": "node server/index.js",
    "sync:coros": "node scripts/sync-coros.mjs",
    "sync:strava": "node scripts/strava-snapshot.mjs",
    "build:snapshot": "node scripts/build-snapshot.mjs",
    "build:ppg": "node scripts/build-ppg-library.mjs",
    "build:club": "node scripts/build-club-sessions.mjs",
    "push:snapshot": "node scripts/push-snapshot.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
